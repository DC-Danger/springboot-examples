# elasticsearch配置
# 9200端口是用来让HTTP REST API来访问ElasticSearch，而9300端口是传输层监听的默认端口
spring:
  data:
    elasticsearch:
      cluster-name: my-application # 集群名称
      cluster-nodes: 127.0.0.1:9300 # 集群地址，可多个，逗号分隔